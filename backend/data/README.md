# Data

The prepared dataset will be placed here:

- `library_combined_dataset.csv`

This flat CSV is **not** the database itself. A future import script will transform it into normalized Neon PostgreSQL tables.

Do not import the dataset until the schema is finalized.
